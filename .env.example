# Stripe — shop catalog + checkout (Netlify env vars)
STRIPE_SECRET_KEY=sk_test_...

# Local dev with functions: netlify dev → http://localhost:8888
# See docs/stripe-products.md for Product metadata fields
